<!DOCTYPE html>
<html>
<head>
    <title>Get file names</title>
    <script>
        function disconnect() {
            window.location.href = 'goodbye.html';
        }
    </script>
</head>
<body>
    <h2>Hello dear Engineer</h2>
    <form method="post" action="<?php echo htmlspecialchars
            ($_SERVER["PHP_SELF"]); ?>">
        <label for="command">Enter File name:</label>
        <input type="text" id="command" name="command">
        <input type="submit" value="Execute">
    </form>
    <button onclick="disconnect()">Disconnect</button>

    <?php
    if ($_SERVER["REQUEST_METHOD"] == "POST") {
        $command = $_POST['command'];
        $fullCommand = "ls " . $command;
        openlog("php_app", LOG_PID | LOG_PERROR, LOG_USER); 
        syslog(LOG_INFO, "Command received: " . $fullCommand); 
        closelog(); // Close the syslog connection
        echo $fullCommand;
        $output = shell_exec($fullCommand);
        echo "<pre>$output</pre>";
    }
    ?>
</body>
</html>
